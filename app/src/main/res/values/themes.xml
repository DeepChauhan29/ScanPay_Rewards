<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (Light Theme) -->
    <style name="Theme.QRCodeScanner" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/white</item>
        
        <!-- Secondary brand colors -->
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorSecondaryVariant">@color/colorAccentDark</item>
        <item name="colorOnSecondary">@color/white</item>
        
        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        
        <!-- Background colors -->
        <item name="android:colorBackground">@color/backgroundPrimaryLight</item>
        <item name="colorSurface">@color/backgroundSecondaryLight</item>
        <item name="colorOnSurface">@color/textPrimary</item>
        
        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/textPrimary</item>
        <item name="android:textColorSecondary">@color/textSecondary</item>
        <item name="android:textColorHint">@color/textHintLight</item>
        
        <!-- Card colors -->
        <item name="cardBackgroundColor">@color/white</item>
        <item name="cardElevation">2dp</item>
        <item name="cardCornerRadius">8dp</item>
        
        <!-- Button colors -->
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
        
        <!-- Floating Action Button colors -->
        <item name="floatingActionButtonStyle">@style/Widget.App.FloatingActionButton</item>
        
        <!-- Bottom navigation theme -->
        <item name="bottomNavigationStyle">@style/Widget.App.BottomNavigationView.Light</item>

        <!-- Input colors -->
        <item name="textInputStyle">@style/Widget.App.TextInputLayout.Light</item>
    </style>

    <!-- Button style -->
    <style name="Widget.App.Button" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/colorPrimary</item>
        <item name="android:textColor">@color/white</item>
        <item name="rippleColor">@color/rippleDark</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Floating Action Button style -->
    <style name="Widget.App.FloatingActionButton" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="backgroundTint">@color/colorPrimary</item>
        <item name="tint">@color/white</item>
        <item name="rippleColor">@color/rippleDark</item>
    </style>

    <!-- Bottom Navigation style (Light) -->
    <style name="Widget.App.BottomNavigationView.Light" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.BottomNavigationView.Light</item>
        <item name="android:background">@color/white</item>
        <item name="itemIconTint">@color/bottom_nav_color</item>
        <item name="itemTextColor">@color/bottom_nav_color</item>
    </style>

    <style name="ThemeOverlay.App.BottomNavigationView.Light" parent="">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/textSecondary</item>
    </style>

    <!-- Text Input style (Light) -->
    <style name="Widget.App.TextInputLayout.Light" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/colorPrimary</item>
        <item name="hintTextColor">@color/colorPrimary</item>
        <item name="android:textColorHint">@color/textHintLight</item>
        <item name="boxBackgroundColor">@color/backgroundPrimaryLight</item>
    </style>

    <!-- Base Text Input Layout Style (will inherit from light version) -->
    <style name="Widget.App.TextInputLayout" parent="Widget.App.TextInputLayout.Light">
        <!-- This style inherits all properties from light version -->
    </style>

    <!-- Base Bottom Navigation Style (will inherit from light version) -->
    <style name="Widget.App.BottomNavigationView" parent="Widget.App.BottomNavigationView.Light">
        <!-- This style inherits all properties from light version -->
    </style>
</resources>